version: '3.8'

services:
  discord-bot:
    build: .
    container_name: lain-ctf-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - TZ=UTC

